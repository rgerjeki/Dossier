"""Dossier: an offline OSINT investigation workbench.

The engine (models, case storage, collectors, report render) is intentionally
independent of the Qt UI so it can be built and tested on its own.
"""

__version__ = "0.1.0"
